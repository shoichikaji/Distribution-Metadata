prereqs:
  runtime:
    requires:
      CPAN::DistnameInfo:
      CPAN::Meta:
      ExtUtils::Packlist:
      JSON:
      Module::Metadata:
      perl: { version: 'v5.16' }
  develop:
    requires:
      File::pushd:
